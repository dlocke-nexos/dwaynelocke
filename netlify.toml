[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Decap CMS admin at /admin — enable Identity + Git Gateway in Netlify dashboard.
# See docs/ADMIN-SETUP.md
